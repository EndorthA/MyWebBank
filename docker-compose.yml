services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: webbank
      POSTGRES_PASSWORD: webbank
      POSTGRES_DB: webbank
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: